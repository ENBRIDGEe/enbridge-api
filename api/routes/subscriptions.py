"""Reserved for future subscription endpoints.

The subscriptions table exists in the schema, but subscription API behavior is
outside the current PRD/MVP route surface and is not included in ``main.py``.
"""
